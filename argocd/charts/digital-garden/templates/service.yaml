apiVersion: v1
kind: Service
metadata:
  name: {{ include "digital-garden.fullname" . }}
  labels:
    {{- include "digital-garden.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digital-garden.selectorLabels" . | nindent 4 }}
