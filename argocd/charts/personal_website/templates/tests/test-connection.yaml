apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "personal_website.fullname" . }}-test-connection"
  labels:
    {{- include "personal_website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "personal_website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
