apiVersion: v1
kind: Service
metadata:
  name: {{ include "toktoktok.fullname" . }}
  labels:
    {{- include "toktoktok.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "toktoktok.selectorLabels" . | nindent 4 }}
